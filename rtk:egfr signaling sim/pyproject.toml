[project]
name = "rtk-egfr"
version = "0.1.0"
description = "rtk/egfr signaling sandbox + streamlit visualizations"
requires-python = ">=3.10"

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
